# Dependency directories
node_modules/
npm-debug.log
yarn-error.log

# Sensitive environment variables (should be passed at runtime, not built into the image)
.env

# Local state and log files (these are handled via Docker volumes)
mirror-state.json
mirror-state.json.tmp
backups.json
backups.json.tmp
processed.json
processed.json.tmp

# Temporary video files generated by the bot
temp_*.mp4

# Git and Docker configuration files
.git
.gitignore
Dockerfile
docker-compose.yml