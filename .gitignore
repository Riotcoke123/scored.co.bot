# Dependencies
node_modules/
npm-debug.log
yarn-error.log

# Secrets — never commit real credentials
.env

# Runtime state and logs (persisted via Docker volumes / pm2)
mirror-state.json
mirror-state.json.tmp
backups.json
backups.json.tmp
processed.json
processed.json.tmp
logs/

# Temporary video files generated by the bot
temp_*.mp4

# OS/editor cruft
.DS_Store
